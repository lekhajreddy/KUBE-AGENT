apiVersion: v1
kind: Secret
metadata:
  name: {{ include "kubemind-agent.fullname" . }}
  labels:
    {{- include "kubemind-agent.labels" . | nindent 4 }}
type: Opaque
stringData:
  api-key: {{ .Values.agent.apiKey | quote }}
